import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import PinInput from '../../components/PinInput.vue';

<Meta
	title="Components/Forms/PinInput"
	component={ PinInput }
	argTypes={{
		state: {
			control:{
				type: 'select',
				options: [
					'default',
					'valid',
					'invalid',
				],
			}
		},
	}}
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ 
`<cds-pin-input />`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { CdsPinInput: PinInput },
	setup() {
		return { args };
	},
	data() {
		return {
			value: '',
		};
	},
	template: /*html*/ `
		<cds-pin-input
			v-bind="args"
			v-model="value"
		/>
	`,
	updated() {
		console.info('%cv-model: ', 'color: #2C70CD;', this.value);
	},
});

# PinInput

### PinInputs are components used to add one-time passwords and 2FA codes
---
<br />

## Preview

<Canvas>
	<Story
		name="PinInput"
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="PinInput" />