import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import FlatButton from '../../components/FlatButton.vue';

<Meta
	title="Components/Forms/FlatButton"
	component={ FlatButton }
	argTypes={{
		variant: {
			control:{
				type: 'select',
				options: [
					'green',
					'teal',
					'turquoise',
					'blue',
					'indigo',
					'violet',
					'pink',
					'red',
					'orange',
					'amber'
				],
			}
		},
	}}
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ 
`<cds-flat-button
	variant="green"
	text="Link Button"
/>`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { CdsFlatButton: FlatButton },
	setup() {
		return { args };
	},
	template: /*html*/ `
		<cds-flat-button
			v-bind="args" 
		>
		</cds-flat-button>`,
});

# FlatButton

### FlatButtons are components similar to links, but that function like buttons.
---
<br />

## Preview

<Canvas>
	<Story
		name="FlatButton"
		args={{
			variant: 'green',
			text: 'Link Button',
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="FlatButton" />
