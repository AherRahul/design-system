import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import Badge from '../../components/Badge.vue';

<Meta
	title="Components/Display/Badge"
	component={ Badge }
	argTypes={{
		variant: {
			control:{
				type: 'select',
				options: [
					'green',
					'teal',
					'turquoise',
					'blue',
					'indigo',
					'violet',
					'pink',
					'red',
					'orange',
					'amber',
					'gray',
					'white',
				],
			}
		},
		size: {
			control:{
				type: 'select',
				options: [
					'sm',
					'md',
					'lg',
				],
			}
		},
	}}
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ 
`<cds-badge
	variant="red"
	size="md"
>
	Badge
</cds-badge>`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { CdsBadge: Badge },
	setup() {
		return { args };
	},
	template: /*html*/ `
		<cds-badge
			v-bind="args"
			@click="handleEmit"
		>
			Badge
		</cds-badge>`,
	methods: {
		handleEmit(value) { console.info('value: ', value); }
	}
});


# Badge

### Badges are status indicators used to make important metadata evident.
---
<br />

## When to use:
- It is necessary to show statuses associated with business logic or user actions.
- The content to be shown is readonly .
- You need to categorize something.


<br />

## When not to use:
- The content that the badge represents can be set or removed by the user.
- Clicking on the component needs to perform an action or functionality.


<br />

## Preview

<Canvas>
	<Story
		name="Badge"
		args={{
			variant: 'red',
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="Badge" />
