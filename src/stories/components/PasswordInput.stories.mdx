import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import PasswordInput from '../../components/PasswordInput.vue';

<Meta
	title="Components/Forms/PasswordInput"
	component={ PasswordInput }
	argTypes={{
		state: {
			control:{
				type: 'select',
				options: [
					'default',
					'valid',
					'invalid',
				],
			}
		},
	}}
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ `
                <cds-password-input />`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { CdsPasswordInput: PasswordInput },
	setup() {
		return { args };
	},
	data() {
		return {
			value: '',
		};
	},
	template: /*html*/ `
		<cds-password-input
			v-bind="args" 
			v-model="value"
		/>`,
	updated() {
		console.info('%cv-model: ', 'color: #2C70CD;', this.value);
	},
});

# PasswordInput

### PasswordInputs allow users to enter sensitive characters into the interface.
---
<br />

## When to use:
- Require a text field to enter sensitive information, such as a password.

<br />

## When not to use:
- The data to be used in the component is common data (Example: name or age).

<br />

## Preview

<Canvas>
	<Story
		name="PasswordInput"
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="PasswordInput" />