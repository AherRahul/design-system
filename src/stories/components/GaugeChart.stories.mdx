import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import GaugeChart from '../../components/GaugeChart.vue';

<Meta
	title="Components/Charts/GaugeChart"
	component={ GaugeChart }
	argTypes={{
		variant: {
			control:{
				type: 'select',
				options: [
					'green',
					'teal',
					'turquoise',
					'blue',
					'indigo',
					'violet',
					'pink',
					'red',
					'orange',
					'amber',
					'dark'
				],
			}
		},
		value: {
			control: {
				type: 'range',
				min: 0,
				max: 100,
				step: 1,
			}
		},
	}}
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/
`<cds-gauge-chart
	variant="green"
	:size="300"
	:value="79.2"
/>`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { CdsGaugeChart: GaugeChart },
	setup() {
		return { args };
	},
	template: /*html*/
		`<cds-gauge-chart
			v-bind="args"
		/>`,
});

<div align="center" style={{display: 'flex'}}>
    <a href="https://rahulaher.netlify.app">
        <img src="https://rahulaher.netlify.app/img/logo/glyph-black-colored.svg" alt="Take care soon" width="50" />
        <img src="https://rahulaher.netlify.app/img/logo/word-black-colored.svg" alt="Take care soon" width="150" />
    </a>
</div>

<br />
<br />

# GaugeChart

### The Gauge Chart is designed to show progress towards a specific goal.
---
<br />

## When to use:
- Use "Gauge charts" when you need to monitor and visually communicate the status of key metrics or performance indicators;
- Use "Gauge charts" to communicate the overall state of a single metric in a quick and visually impactful way.

<br />

## When not to use:
- Avoid using "Gauge charts" to represent complex or multidimensional data sets. This type of chart is most effective when displaying a single metric against a scale.

<br />

## Preview

<Canvas>
	<Story
		name="Gauge Chart"
		args={{
			value: 79.2,
			variant: "green",
			size: 300,
			subtitle: 'suitable for the indicator'
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="Gauge Chart" />
