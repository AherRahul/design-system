import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import Spinner from '../../components/Spinner.vue';

<Meta
	title="Components/Loaders/Spinner"
	component={ Spinner }
	argTypes={{
		size: {
			control: {
				type: 'select',
				options: ['sm', 'md', 'lg'],
			}
		},
		variant: {
			control: {
				type: 'select',
				options: [
					'green',
					'teal',
					'turquoise',
					'blue',
					'indigo',
					'violet',
					'pink',
					'red',
					'orange',
					'amber',
					'white',
				],
			}
		},
	}}
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ 
`<cds-spinner
	variant="green"
	size="md"
/>`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { CdsSpinner: Spinner },
	setup() {
		return { args };
	},
	template: /*html*/ `
		<cds-spinner
			v-bind="args"
		/>
	`,
});

# Spinner

### Spinners is a component used to indicate that the progress of an operation has not yet been completed.
---
<br />

## When to use:
- It is necessary to prevent the viewing and navigation of page content while there is a pending operation.

<br />

## When not to use:
- Viewing and navigation cannot be interrupted.

<br />

## Preview

<Canvas>
	<Story
		name="Spinner"
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="Spinner" />