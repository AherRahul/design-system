import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import Dropdown from '../../components/Dropdown.vue';

<Meta
	title="Components/Display/Dropdown"
	component={ Dropdown }
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ 
`<cds-dropdown
	label="Idade"
	content="0 a 120 anos"
	@click="isActive = !isActive"
/>`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { CdsDropdown: Dropdown },
	setup() {
		return { args };
	},
	template: /*html*/ `
		<cds-dropdown
			v-bind="$props"
		>
			<p>Dropdown Slot</p>
		</cds-dropdown>`,
});


# Dropdown

### Dropdown are components used to activate popovers containing filters for lists and views.
---
<br />

## When to use:
- For use with filters.
- There are popovers containing filtering options.


<br />

## When not to use:
- Replacing a Multiselect or Select.

<br />

## Preview

<Canvas>
	<Story
		name="Dropdown"
		args={{
			label: 'Age',
			content: '0 to 120 years',
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="Dropdown" />
