import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import LoadingBar from '../../components/LoadingBar.vue';

<Meta
	title="Components/Loaders/LoadingBar"
	component={ LoadingBar }
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ 
`<cds-loading-bar />`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { CdsLoadingBar: LoadingBar },
	setup() {
		return { args };
	},
	template: /*html*/ `
		<center>
			<cds-loading-bar
				v-bind="args"
			/>
		</center>
	`,
});

# LoadingBar

### LoadingBars is a component used to indicate that the progress of an operation has not yet been completed.
---
<br />

## When to use:
- It is necessary to prevent the viewing and navigation of page content while there is a pending operation.

<br />

## When not to use:
- Viewing and navigation cannot be interrupted.

<br />

## Preview

<Canvas>
	<Story
		name="LoadingBar"
		
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="LoadingBar" />