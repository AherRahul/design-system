import { Meta, Story, Props, ArgsTable, Canvas, Preview, Source } from '@storybook/addon-docs';
import ActionBar from '../../components/ActionBar.vue';
import Button from '../../components/Button.vue';

<Meta
	title="Components/Display/ActionBar"
	component={ ActionBar }
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ `
<cds-action-bar
	v-bind="args"
	:show="showActionBar"
	@click="logAction"
	@close="logClose"
>
	<template #description>
		8 selected items
	</template>
</cds-action-bar>`
			},
		}}
	}
/>

export const Template = (args) => ({
	setup() {
		return { args };
	},
	data() {
		return {
			showActionBar: false,
		};
	},
	components: { CdsActionBar: ActionBar, CdsButton: Button },
	template: /*html*/ `
		<div>
			<cds-button
				id="trigger-popover"
				@click="togglePopover"
			>
				Click
			</cds-button>
			<cds-action-bar
				v-bind="args"
				:show="showActionBar"
				@click="logAction"
				@close="logClose"
			>
				<template #description>
					8 selected items
				</template>
			</cds-action-bar>
		</div>
	`,
	methods: {
		logAction(btn) {
			console.info('logAction: ', btn);
		},
		logClose(event) {
			this.showActionBar = false;
			console.info('logClose: ', event);
		},
		togglePopover() {
			this.showActionBar = true;
		},
	},
});


<div align="center" style={{display: 'flex'}}>
    <a href="https://rahulaher.netlify.app">
        <img src="https://rahulaher.netlify.app/img/logo/glyph-black-colored.svg" alt="Take care soon" width="50" />
        <img src="https://rahulaher.netlify.app/img/logo/word-black-colored.svg" alt="Take care soon" width="150" />
    </a>
</div>

<br />
<br />


# ActionBar

### ActionBars are toolbars that can display actions and information to the user.
---
<br />

## When to use:
- To group actions that the user can take on items in a list or table.
- There is no space in menus and other places to display actions and information.

<br />

## When not to use:
- To view system statuses.
- In floating mode, the ActionBar permanently hides information on the screen.

<br />

## Preview

<Canvas>
	<Story
		name="Action Bar"
		args={{
			actions: ['Button1', 'Button2', 'Button3'],
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="Action Bar" />
