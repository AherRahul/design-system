import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import TextArea from '../../components/TextArea.vue';

<Meta
	title="Components/Forms/TextArea"
	component={ TextArea }
	argTypes={{
		state: {
			control: {
				type: 'select',
					options: [
						'default',
						'valid',
						'invalid',
					],
			}
		},
	}}
	parameters={{
		viewMode: 'docs',
		previewTabs: { canvas: { hidden: true }},
		docs: {
			source: {
				language: 'html',
				format:true,
				code: /*html*/ `
<cds-text-area />`
			},
		}
	}}
/>

export const Template = (args) => ({
	components: { CdsTextArea: TextArea },
	setup() {
		return { args };
	},
	data() {
		return {
			value: '',
		};
	},
	template: /*html*/ `
		<cds-text-area
			v-bind="args" 
			v-model="value"
		/>
	`,
	updated() {
		console.info('%cv-model: ', 'color: #2C70CD;', this.value);
	},
});

# TextArea

### TextAreas allow users to enter long text into an interface.
---
<br />

## When to use:
- Require a text field to enter large information. Example: Product description or messages.

<br />

## When not to use:
- It is only necessary to enter specific data in the text field.

<br />

## Preview

<Canvas>
	<Story
		name="TextArea"
		args={{}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="TextArea" />
